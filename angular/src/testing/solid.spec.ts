it('should single responsibility principle', async {
	
	const UM_METODO_NO_MAXIMO = 1;
	const typescript = require('typescript');
	const product = require('./products/product');
	let cont = 0;
	
	for(int i=0; i<typescript.getElementsByClassName(require('./products/product')); i++){
		
		cont++;
		
		if(cont > 1){
			
			console.log('srp fail');
			
			fail(true);
			
			break;
		}
	}
	console.log('srp sucess');
	}
);

it('should open closed', async {
	
	}
);

it('should liskov substitution principle', async {
	
	}
);

it('should interface segregation principle', async {
	
	
	
	}
);

it('should dependency inversion principle', async {
	
	}
);